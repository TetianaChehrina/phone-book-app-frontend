export const selectNameFilter = (state) => state.filters.name;
export const selectNumberFilter = (state) => state.filters.number;
export const selectContactTypeFilter = (state) => state.filters.contactType;
export const selectIsFavouriteFilter = (state) => state.filters.isFavourite;
